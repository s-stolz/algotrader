# Shared constraints across Python services.
# Keep versions aligned here when possible.
# Use <service>/constraints.override.txt only for justified exceptions.

fastapi==0.128.0
uvicorn==0.40.0
pydantic==2.12.2
pydantic-settings==2.12.0
python-dotenv==1.2.1
redis==7.1.0
httpx==0.28.1
numpy==2.2.6
pandas==2.3.2
watchdog==3.0.0
websockets==16.0
